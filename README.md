# 密折司 (MiZheSi) - Prompt Inspector Plugin

这是一个从 **Amily2号聊天优化助手** 中提取出来的独立 SillyTavern 插件。

## 功能说明

**密折司**是一个 Prompt 检查器，让你能够在发送生成请求之前查看和编辑完整的 Prompt 内容。

### 主要功能

- 📜 **Prompt 预览**：在发送请求前查看完整的 Prompt 内容
- ✏️ **实时编辑**：支持直接修改 Prompt 内容
- 🔢 **Token 计数**：实时显示每条消息和总体的 Token 数量
- 🔍 **内容搜索**：快速搜索并定位特定内容
- 🎨 **角色区分**：用不同颜色区分 system/user/assistant 消息
- ⛔ **取消生成**：支持在检查时取消生成请求

### 支持的 API 类型

- ✅ Chat Completion API (OpenAI 格式)
- ✅ Text Generation API

## 安装方法

1. 将 `ST-MiZheSi-Plugin` 文件夹复制到 SillyTavern 的 `public/scripts/extensions/third-party/` 目录下
2. 重启 SillyTavern
3. 插件会自动加载

## 使用方法

1. 在 SillyTavern 左下角的扩展菜单中找到 **"开启密折司"** 按钮
2. 点击按钮开启/关闭功能
3. 开启后，每次发送消息前都会弹出 Prompt 检查窗口
4. 在窗口中可以：
   - 点击消息标题栏展开/折叠消息内容
   - 直接编辑 textarea 中的内容
   - 使用搜索功能查找特定内容
   - 点击"确认修改"发送修改后的内容
   - 点击"放弃修改"发送原始内容
   - 点击"取消生成"停止请求

## 文件结构

```
ST-MiZheSi-Plugin/
├── manifest.json    # 插件配置文件
├── index.js         # 插件主逻辑
├── template.html    # UI 模板和样式
└── README.md        # 说明文档
```

## 版本要求

- SillyTavern 版本 >= 1.10.0

## 原项目

此插件提取自 [ST-Amily2-Chat-Optimisation](https://github.com/Wx-2025/ST-Amily2-Chat-Optimisation.git)

## 作者

- 原作者: Wx-2025
- 插件提取: Assistant

## 许可证

遵循原项目许可证
